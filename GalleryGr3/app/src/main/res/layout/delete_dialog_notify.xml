<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="5dp"
    >
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Delete"
        android:textAlignment="center"
        android:textSize="30dp"
        android:textStyle="bold"
        android:id="@+id/titleBox">

    </TextView>
    <TextView
        android:id="@+id/deleteNotify"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Do you want to delete 0 image(s) permanently in device ?"
        android:paddingHorizontal="50dp"
        android:layout_marginVertical="10dp">
    </TextView>
    <TableLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:stretchColumns="0,1"
        android:paddingHorizontal="30dp">
        <TableRow>
            <Button android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="cancel"
                    android:backgroundTint="@color/cancel_delete"
                    android:textColor="@color/black"
                    android:id="@+id/cancelDelete"
                android:layout_marginHorizontal="20dp"> </Button>

            <Button android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Delete"
                android:backgroundTint="@color/confirm_delete"
                android:textColor="@color/white"
                android:id="@+id/confirmDelete"
                android:layout_marginHorizontal="20dp"
                > </Button>
        </TableRow>
    </TableLayout>

</LinearLayout>